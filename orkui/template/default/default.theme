<!DOCTYPE html>
<html>
	<head>
		<title><?php echo (strlen($page_title)>0?"ORK 3: ":"ORK 3").ucwords($page_title); ?></title>
		<link rel="icon" href="/favicon.jpeg">
		<?php if ($no_index) : ?>
		<meta name="robots" content="noindex">
		<?php endif ; ?>
		<meta name=viewport content="user-scalable=no,width=device-width,minimum-scale=1" />
		<meta name="description" content="The Online Record Keeper for the Amtgard International LARP.">
		<link type="text/css" href="<?=HTTP_TEMPLATE;?>default/style/orkui.css?v=<?=filemtime(DIR_TEMPLATE.'default/style/orkui.css')?>" rel="stylesheet" />

    	<link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.2/css/all.css" integrity="sha384-oS3vJWv+0UjzBfQzYUhtDYW+Pj2yciDJxpsK1OYPAYjqT085Qq/1cq5FLXAZQ7Ay" crossorigin="anonymous">
		<script type="text/javascript" src="<?=HTTP_TEMPLATE;?>default/script/orkui.js"></script>
		<!-- Google tag (gtag.js) -->
		<script async src="https://www.googletagmanager.com/gtag/js?id=G-PVQCKENY0M"></script>
		<script>
		window.dataLayer = window.dataLayer || [];
		function gtag(){dataLayer.push(arguments);}
		gtag('js', new Date());

		gtag('config', 'G-PVQCKENY0M');
		</script>
		<!-- Cloudflare Web Analytics --><script defer src='https://static.cloudflareinsights.com/beacon.min.js' data-cf-beacon='{"token": "b2fb40110e5740e2ac1aeffe77a18936"}'></script><!-- End Cloudflare Web Analytics -->
	<style>
	/* ============================================================
	   ORK Help Panel — Gmail compose-style slide-up
	   ============================================================ */
	.nav-help-btn {
		background: none; border: none; cursor: pointer;
		color: rgba(255,255,255,0.55); font-size: 16px;
		width: 36px; height: 36px; border-radius: 50%;
		display: inline-flex; align-items: center; justify-content: center;
		transition: color 0.15s, background 0.15s; flex-shrink: 0;
	}
	.nav-help-btn:hover, .nav-help-btn.ork-help-active {
		color: #fff; background: rgba(255,255,255,0.18);
	}
	#ork-help-panel {
		position: fixed; bottom: 0; right: 80px;
		width: 380px; max-height: 72vh;
		background: #fff; border-radius: 10px 10px 0 0;
		box-shadow: 0 -4px 24px rgba(0,0,0,0.22);
		transform: translateY(100%);
		transition: transform 0.28s cubic-bezier(0.4,0,0.2,1);
		z-index: 8500;
		display: flex; flex-direction: column;
	}
	#ork-help-panel.ork-help-open { transform: translateY(0); }
	#ork-help-panel.ork-help-minimized { transform: translateY(calc(100% - 46px)); }
	.ork-help-header {
		display: flex; align-items: center; justify-content: space-between;
		padding: 0 14px; height: 46px;
		background: #2b4c7e; border-radius: 10px 10px 0 0;
		cursor: pointer; flex-shrink: 0;
	}
	.ork-help-header-title {
		display: flex; align-items: center; gap: 8px;
		color: #fff; font-weight: 600; font-size: 14px; user-select: none;
	}
	.ork-help-header-title i { font-size: 15px; }
	.ork-help-scope-badge {
		background: rgba(255,255,255,0.18); border-radius: 10px;
		padding: 1px 9px; font-size: 11px; font-weight: 500; color: rgba(255,255,255,0.9);
	}
	.ork-help-header-controls { display: flex; gap: 2px; }
	.ork-help-header-controls button {
		background: none; border: none; cursor: pointer;
		color: rgba(255,255,255,0.7); width: 30px; height: 30px;
		border-radius: 50%; display: flex; align-items: center;
		justify-content: center; font-size: 13px; transition: background 0.15s, color 0.15s;
	}
	.ork-help-header-controls button:hover { background: rgba(255,255,255,0.2); color: #fff; }
	.ork-help-body {
		overflow-y: auto; padding: 16px; flex: 1; min-height: 0;
		-webkit-overflow-scrolling: touch;
	}
	/* Sections */
	.ork-help-section { margin-bottom: 18px; }
	.ork-help-section-label {
		font-size: 10.5px; font-weight: 700; text-transform: uppercase;
		letter-spacing: 0.09em; color: #718096; margin-bottom: 8px;
		padding-bottom: 5px; border-bottom: 1px solid #e2e8f0;
	}
	/* Accordion items */
	.ork-help-item {
		border: 1px solid #e2e8f0; border-radius: 7px;
		margin-bottom: 6px; overflow: hidden;
	}
	.ork-help-item-trigger {
		display: flex; align-items: center; justify-content: space-between;
		padding: 10px 12px; cursor: pointer;
		font-size: 13px; font-weight: 500; color: #2d3748;
		background: #f7fafc; border: none; width: 100%; text-align: left;
		transition: background 0.12s;
	}
	.ork-help-item-trigger:hover { background: #edf2f7; }
	.ork-help-item-trigger-left { display: flex; align-items: center; gap: 0; }
	.ork-help-item-icon { color: #3a7bd5; width: 20px; text-align: center; margin-right: 9px; }
	.ork-help-chevron { color: #a0aec0; font-size: 11px; transition: transform 0.2s; }
	.ork-help-item.ork-help-expanded .ork-help-chevron { transform: rotate(180deg); }
	.ork-help-item-body {
		display: none; padding: 12px 14px;
		font-size: 13px; color: #4a5568; line-height: 1.65;
		border-top: 1px solid #e2e8f0; background: #fff;
	}
	.ork-help-item.ork-help-expanded .ork-help-item-body { display: block; }
	.ork-help-item-body p { margin: 0 0 8px 0; }
	.ork-help-item-body p:last-child { margin-bottom: 0; }
	.ork-help-item-body ul { margin: 6px 0 8px 0; padding-left: 18px; }
	.ork-help-item-body li { margin-bottom: 5px; }
	.ork-help-tip {
		background: #ebf4ff; border-left: 3px solid #3a7bd5;
		padding: 8px 10px; border-radius: 0 5px 5px 0; margin-top: 10px;
		font-size: 12px; color: #2c5282;
	}
	.ork-help-tip i { margin-right: 4px; }
	.ork-help-empty { color: #718096; font-size: 13px; text-align: center; padding: 24px 0; }
	</style>
	</head>
	<body>
		<div id='theme_container'>

<?php
	echo $CONTROLLER_CONTENTS;
?>

		</div>


	<nav id='newmenu'>
		<a id='nav-mobile-home' href='<?=UIR?>'><i class='fas fa-home'></i></a>
		<!-- Left: breadcrumb navigation -->
		<?php if (isset($menu) && is_array($menu) && count($menu) > 0) : ?>
		<div id='navmenu'>
			<ul>
<?php foreach ($menu as $k => $item) : ?>
<?php if ($k === 'admin') continue; ?>
				<li><a href='<?=$item["url"] ?>'><?=$item["display"] ?></a> <span class='breadcrumb-trail'><?=(isset($item['no-crumb']) || $item === end($menu))?'':'<i class="fas fa-angle-double-right"></i>'; ?></span> </li>
<?php endforeach; ?>
			</ul>
		</div>
		<?php else : ?>
		<div id='navmenu'></div>
		<?php endif; ?>

		<!-- Right: controls -->
		<div id='controls'>
			<!-- Universal search -->
			<div id='nav-search-wrap'>
				<a class='nav-icon-btn' id='nav-search-toggle' onclick='nsToggle(this)' title='Search'><i class='fas fa-search'></i></a>
				<div id='nav-search-input-wrap'>
					<input id='UniversalSearch' placeholder='Search active players, parks, kingdoms&hellip;' class='nav-search-input' autocomplete='off' />
					<div id='nav-search-dropdown'></div>
				</div>
			</div>

			<!-- Home Kingdom / Home Park quick-links -->
<?php if ($this->__session->token != null) : ?>
<?php
	global $DB;
	$__huid = (int)$this->__session->user_id;
	$__homeResult = $DB->DataSet("SELECT p.park_id, p.name as park_name, k.kingdom_id, k.name as kingdom_name FROM ork_mundane m LEFT JOIN ork_park p ON m.park_id = p.park_id LEFT JOIN ork_kingdom k ON p.kingdom_id = k.kingdom_id WHERE m.mundane_id = $__huid LIMIT 1");
	$__home = null;
	if ($__homeResult && $__homeResult->Next() && $__homeResult->park_id) {
		$__home = ['park_id' => $__homeResult->park_id, 'park_name' => $__homeResult->park_name, 'kingdom_id' => $__homeResult->kingdom_id, 'kingdom_name' => $__homeResult->kingdom_name];
	}
?>
<?php if ($__home) : ?>
			<a href='<?=UIR?>Kingdom/profile/<?=(int)$__home['kingdom_id']?>' class='nav-home-btn' title='My Kingdom: <?=htmlspecialchars($__home['kingdom_name'])?>'>
				<i class='fas fa-chess-rook'></i>
				<span class='nav-home-label'><?=htmlspecialchars($__home['kingdom_name'])?></span>
			</a>
			<a href='<?=UIR?>Park/profile/<?=(int)$__home['park_id']?>' class='nav-home-btn' title='My Park: <?=htmlspecialchars($__home['park_name'])?>'>
				<i class='fas fa-tree'></i>
				<span class='nav-home-label'><?=htmlspecialchars($__home['park_name'])?></span>
			</a>
<?php endif; ?>
<?php endif; ?>

			<!-- Help button -->
			<button class='nav-help-btn' id='ork-help-nav-btn' onclick='orkHelpToggle()' title='Help'><i class='fas fa-question-circle'></i></button>

			<!-- Resources dropdown -->
			<div class='nav-dropdown-wrap' id='nav-resources-wrap'>
				<button class='nav-icon-btn' onclick="document.getElementById('nav-resources-wrap').classList.toggle('open')" title='Resources'><i class='fas fa-book'></i></button>
				<div class='nav-dropdown-menu' style='min-width:200px'>
					<a href='https://www.amtgard.com/' target='_blank'><i class='fas fa-globe'></i> Amtgard Website</a>
					<a href='https://wiki.amtgard.com/Main_Page' target='_blank'><i class='fas fa-book-open'></i> Amtgard Wiki</a>
					<a href='https://play.amtgard.com/' target='_blank'><i class='fas fa-map-marker-alt'></i> Find a Chapter</a>
					<div class='nav-dropdown-divider'></div>
					<a href='#' onclick="navInfoDialog('New Documentation Coming Soon!');return false;"><i class='fas fa-book'></i> ORK Documentation</a>
				</div>
			</div>

<?php if ($this->__session->token == null) : ?>
			<a href='<?=UIR;?>Login' class='nav-login-btn'><i class='fas fa-sign-in-alt'></i> Login</a>
<?php else : ?>
<?php
	$__navUid      = (int)$this->__session->user_id;
	$__navImgName  = sprintf('%06d', $__navUid);
	$__navHasPhoto = file_exists(DIR_PLAYER_IMAGE . $__navImgName . '.png') || file_exists(DIR_PLAYER_IMAGE . $__navImgName . '.jpg');
	$DB->Clear();
	$__personaRow  = $DB->DataSet("SELECT persona FROM ork_mundane WHERE mundane_id = $__navUid LIMIT 1");
	$__navPersona  = ($__personaRow && $__personaRow->Next() && $__personaRow->persona)
		? $__personaRow->persona
		: $this->__session->user_name;
?>
			<div class='nav-dropdown-wrap' id='nav-avatar-wrap'>
				<button class='nav-avatar-btn' onclick="document.getElementById('nav-avatar-wrap').classList.toggle('open')">
					<span class='nav-avatar-img-wrap'>
						<?php if ($__navHasPhoto): ?>
						<img class='nav-avatar-img'
							src='<?= HTTP_PLAYER_IMAGE . Common::resolve_image_ext(DIR_PLAYER_IMAGE, $__navImgName) ?>'
							onerror="this.style.display='none';this.nextElementSibling.style.display='inline-block';"
							alt=''
						/><i class='fas fa-user-circle nav-avatar-fallback-icon'></i>
						<?php else: ?>
						<i class='fas fa-user-circle nav-avatar-fallback-icon' style='display:inline-block'></i>
						<?php endif; ?>
					</span>
					<span class='nav-username'><?=htmlspecialchars($__navPersona)?></span>
					<i class='fas fa-chevron-down nav-chevron'></i>
				</button>
				<div class='nav-dropdown-menu'>
					<a href='<?=UIR?>Player/profile/<?=$__navUid?>'><i class='fas fa-user'></i> View Profile</a>
<?php if (isset($menu['admin'])): ?>
					<?php
						$__adminUrl = $menu['admin']['url'];
						if      (strpos($__adminUrl, 'Admin/player/')   !== false) $__adminLabel = 'Admin Player';
						elseif  (strpos($__adminUrl, 'Admin/kingdom/')  !== false) $__adminLabel = 'Admin Kingdom';
						elseif  (strpos($__adminUrl, 'Admin/park/')     !== false) $__adminLabel = 'Admin Park';
						elseif  (strpos($__adminUrl, 'Admin/event/')    !== false) $__adminLabel = 'Admin Event';
						elseif  (strpos($__adminUrl, 'Admin/unit/')     !== false) $__adminLabel = 'Admin Unit';
						else                                                       $__adminLabel = 'Admin Panel';
					?>
					<div class='nav-dropdown-divider'></div>
					<a href='<?=$__adminUrl?>'><i class='fas fa-cog'></i> <?=$__adminLabel?></a>
<?php endif; ?>
					<div class='nav-dropdown-divider'></div>
					<a href='<?=UIR?>Login/logout' class='nav-dropdown-danger'><i class='fas fa-sign-out-alt'></i> Log Out</a>
				</div>
			</div>
<?php endif; ?>
		</div>
	</nav>

	<div id="sidr">
		<ul>
			<ul>
				<li><a href='https://www.facebook.com/mORKmobile/posts/2275859462673190'>Get the ORK Mobile!</a></li>
<?php foreach ($menu as $k => $item) : ?>
				<li><a href='<?=$item["url"] ?>'><?=$item["display"] ?></a></li>
<?php endforeach; ?>
<?php if ($this->__session->token == null) : ?>
				<li><a href='<?=UIR;?>Login'>Login</a></li>
<?php else : ?>
<?php $__sideUid = (int)$this->__session->user_id; ?>
				<li><a href='<?=UIR;?>Player/profile/<?=$__sideUid?>'><?=htmlspecialchars($this->__session->user_name)?></a></li>
				<li><button onClick='javascript:window.location.href="<?=UIR;?>Login/logout"'>Logout</button></li>
<?php endif; ?>
			</ul>
		</ul>
	</div>

	<!-- =============================================
	     ORK Help Panel
	     ============================================= -->
	<div id='ork-help-panel'>
		<div class='ork-help-header' id='ork-help-header'>
			<div class='ork-help-header-title'>
				<i class='fas fa-question-circle'></i>
				<span>Help</span>
				<span class='ork-help-scope-badge' id='ork-help-scope-badge' style='display:none'></span>
			</div>
			<div class='ork-help-header-controls'>
				<button id='ork-help-minimize-btn' title='Minimize'><i class='fas fa-minus'></i></button>
				<button id='ork-help-close-btn' title='Close'><i class='fas fa-times'></i></button>
			</div>
		</div>
		<div class='ork-help-body' id='ork-help-body'>
			<div class='ork-help-empty'><i class='fas fa-spinner fa-spin'></i>&ensp;Loading help&hellip;</div>
		</div>
	</div>

	<script>
	// ---- Search toggle ----
	function nsToggle(btn) {
		var sw = document.getElementById('nav-search-wrap');
		var isOpen = sw.classList.toggle('nav-search-open');
		btn.classList.toggle('active');
		if (isOpen) {
			setTimeout(function(){ var el = document.getElementById('UniversalSearch'); if (el) el.focus(); }, 50);
		} else {
			var el = document.getElementById('UniversalSearch');
			if (el) el.value = '';
			var dd = document.getElementById('nav-search-dropdown');
			if (dd) { dd.className = ''; dd.innerHTML = ''; }
		}
	}

	// User's home kingdom ID for priority sorting (0 = not logged in / unknown)
	var nsKid = <?= (isset($__home) && $__home) ? (int)$__home['kingdom_id'] : 0 ?>;
	var nsPid = <?= (isset($__home) && $__home) ? (int)$__home['park_id'] : 0 ?>;
	var nsUir = '<?= UIR ?>';

	$(document).ready(function() {
		// ---- Sidr mobile menu ----
		$('#simple-menu').sidr();

		// ---- Dropdowns: close on outside click ----
		$(document).on('click.navDropdowns', function(e) {
			['nav-avatar-wrap', 'nav-resources-wrap'].forEach(function(id) {
				var wrap = document.getElementById(id);
				if (wrap && !wrap.contains(e.target)) wrap.classList.remove('open');
			});
		});

		// ---- Universal search ----
		var nsTimer = null;
		var nsXhr   = null;

		function nsEsc(str) {
			return $('<span>').text(str || '').html();
		}

		function nsSearch(term) {
			var $dd = $('#nav-search-dropdown');
			$dd.html('<div class="nsdd-loading"><i class="fas fa-spinner fa-spin"></i>&ensp;Searching&hellip;</div>').addClass('nsdd-visible');
			if (nsXhr) nsXhr.abort();
			nsXhr = $.getJSON(nsUir + 'SearchAjax/universal&q=' + encodeURIComponent(term) + '&kid=' + nsKid + '&pid=' + nsPid,
				function(res) { nsRender(res || {}); }
			).fail(function(xhr) {
				if (xhr.statusText !== 'abort')
					$dd.html('<div class="nsdd-empty">Search unavailable</div>');
			});
		}

		function nsRender(res) {
			var $dd = $('#nav-search-dropdown');
			var html = '';
			var sep = false;
			var players  = res.players  || [];
			var parks    = res.parks    || [];
			var kingdoms = res.kingdoms || [];
			var units    = res.units    || [];

			if (players.length) {
				html += '<div class="nsdd-section-header"><i class="fas fa-user fa-fw"></i>&ensp;Players</div>';
				$.each(players, function(i, p) {
					var lbl = p.name ? nsEsc(p.name) : '<em class="nsdd-no-value">No Persona</em>';
					var sub = nsEsc((p.abbr || '') + ' \u00b7 ' + (p.park || ''));
					html += '<a class="nsdd-item" href="?Route=Player/profile/' + p.id + '">' +
						'<span class="nsdd-item-icon"><i class="fas fa-user fa-fw"></i></span>' +
						'<span class="nsdd-item-content"><div class="nsdd-item-label">' + lbl + '</div>' +
						'<div class="nsdd-item-sub">' + sub + '</div></span></a>';
				});
				sep = true;
			}

			if (parks.length) {
				if (sep) html += '<div class="nsdd-divider"></div>';
				html += '<div class="nsdd-section-header"><i class="fas fa-tree fa-fw"></i>&ensp;Parks</div>';
				$.each(parks, function(i, p) {
					html += '<a class="nsdd-item" href="?Route=Park/profile/' + p.id + '">' +
						'<span class="nsdd-item-icon"><i class="fas fa-tree fa-fw"></i></span>' +
						'<span class="nsdd-item-content"><div class="nsdd-item-label">' + nsEsc(p.name) + '</div></span></a>';
				});
				sep = true;
			}

			if (kingdoms.length) {
				if (sep) html += '<div class="nsdd-divider"></div>';
				html += '<div class="nsdd-section-header"><i class="fas fa-crown fa-fw"></i>&ensp;Kingdoms</div>';
				$.each(kingdoms, function(i, k) {
					html += '<a class="nsdd-item" href="?Route=Kingdom/profile/' + k.id + '">' +
						'<span class="nsdd-item-icon"><i class="fas fa-crown fa-fw"></i></span>' +
						'<span class="nsdd-item-content"><div class="nsdd-item-label">' + nsEsc(k.name) + '</div></span></a>';
				});
				sep = true;
			}

			if (units.length) {
				if (sep) html += '<div class="nsdd-divider"></div>';
				html += '<div class="nsdd-section-header"><i class="fas fa-shield-alt fa-fw"></i>&ensp;Companies &amp; Households</div>';
				$.each(units, function(i, u) {
					var icon = (u.unitType === 'Household') ? 'fas fa-home fa-fw' : 'fas fa-shield-alt fa-fw';
					html += '<a class="nsdd-item" href="?Route=Unit/index/' + u.id + '">' +
						'<span class="nsdd-item-icon"><i class="' + icon + '"></i></span>' +
						'<span class="nsdd-item-content"><div class="nsdd-item-label">' + nsEsc(u.name) + '</div>' +
						'<div class="nsdd-item-sub">' + nsEsc(u.unitType || '') + '</div></span></a>';
				});
			}

			if (!html) {
				html = '<div class="nsdd-empty">No results found</div>';
			}

			$dd.html(html);
		}

		(function() {
			var inp = document.getElementById('UniversalSearch');
			if (!inp) return;
			function updatePlaceholder() {
				inp.placeholder = window.innerWidth < 768
					? 'Search\u2026'
					: 'Search active players, parks, kingdoms\u2026';
			}
			updatePlaceholder();
			window.addEventListener('resize', updatePlaceholder);
		})();

		$('#UniversalSearch').on('input', function() {
			var term = $.trim($(this).val());
			clearTimeout(nsTimer);
			if (term.length < 2) {
				$('#nav-search-dropdown').removeClass('nsdd-visible').empty();
				return;
			}
			nsTimer = setTimeout(function() { nsSearch(term); }, 350);
		}).on('keydown', function(e) {
			if (e.key === 'Escape') {
				$('#nav-search-dropdown').removeClass('nsdd-visible').empty();
				document.getElementById('nav-search-wrap').classList.remove('nav-search-open');
				document.getElementById('nav-search-toggle').classList.remove('active');
				$(this).blur();
			} else if (e.key === 'ArrowDown') {
				e.preventDefault();
				$('#nav-search-dropdown .nsdd-item').first().focus();
			}
		});

		$('#nav-search-dropdown').on('keydown', '.nsdd-item', function(e) {
			if (e.key === 'ArrowDown') {
				e.preventDefault();
				$(this).nextAll('.nsdd-item').first().focus();
			} else if (e.key === 'ArrowUp') {
				e.preventDefault();
				var $prev = $(this).prevAll('.nsdd-item').first();
				if ($prev.length) $prev.focus(); else $('#UniversalSearch').focus();
			} else if (e.key === 'Escape') {
				$('#nav-search-dropdown').removeClass('nsdd-visible').empty();
				$('#UniversalSearch').focus();
			}
		});

		// Close search dropdown on outside click
		$(document).on('click.searchDropdown', function(e) {
			if (!$(e.target).closest('#nav-search-wrap').length) {
				$('#nav-search-dropdown').removeClass('nsdd-visible').empty();
			}
		});
	});
	</script>

	<script>
	// =====================================================
	//  ORK Help Panel System
	// ============================================================
	(function() {
		var _panel, _body, _badge, _navBtn;
		var _isOpen = false, _isMinimized = false;

		function _init() {
			_panel  = document.getElementById('ork-help-panel');
			_body   = document.getElementById('ork-help-body');
			_badge  = document.getElementById('ork-help-scope-badge');
			_navBtn = document.getElementById('ork-help-nav-btn');

			document.getElementById('ork-help-close-btn').addEventListener('click', function(e) {
				e.stopPropagation(); _close();
			});
			document.getElementById('ork-help-minimize-btn').addEventListener('click', function(e) {
				e.stopPropagation(); _toggleMinimize();
			});
			document.getElementById('ork-help-header').addEventListener('click', function() {
				if (_isMinimized) _expand();
			});
		}

		function _open() {
			_renderContent();
			_panel.classList.add('ork-help-open');
			_panel.classList.remove('ork-help-minimized');
			_isOpen = true; _isMinimized = false;
			if (_navBtn) _navBtn.classList.add('ork-help-active');
		}

		function _close() {
			_panel.classList.remove('ork-help-open', 'ork-help-minimized');
			_isOpen = false; _isMinimized = false;
			if (_navBtn) _navBtn.classList.remove('ork-help-active');
		}

		function _toggleMinimize() {
			if (_isMinimized) { _expand(); }
			else { _panel.classList.add('ork-help-minimized'); _isMinimized = true; }
		}

		function _expand() {
			_panel.classList.remove('ork-help-minimized'); _isMinimized = false;
		}

		function _esc(s) {
			return String(s || '').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
		}

		function _renderContent() {
			var ctx     = window.orkHelpContext  || {};
			var content = window.orkHelpContent  || [];

			// Update scope badge
			if (_badge) {
				if (ctx.scopeLabel) {
					_badge.textContent = ctx.scopeLabel;
					_badge.style.display = '';
				} else {
					_badge.style.display = 'none';
				}
			}

			// Filter sections & items by context requirements
			var sections = [];
			content.forEach(function(sec) {
				var items = (sec.items || []).filter(function(item) {
					var r = item.requires;
					if (!r) return true;
					if (r === 'own'          && !ctx.isOwnProfile) return false;
					if (r === 'edit'         && !ctx.canEdit)      return false;
					if (r === 'own_or_edit'  && !ctx.isOwnProfile && !ctx.canEdit) return false;
					if (r === 'loggedin'     && !ctx.isLoggedIn)   return false;
					return true;
				});
				if (items.length) sections.push({ section: sec.section, items: items });
			});

			if (!sections.length) {
				_body.innerHTML = '<div class="ork-help-empty">No help topics available for this page.</div>';
				return;
			}

			var html = '';
			sections.forEach(function(sec) {
				html += '<div class="ork-help-section">';
				html += '<div class="ork-help-section-label">' + _esc(sec.section) + '</div>';
				sec.items.forEach(function(item) {
					html += '<div class="ork-help-item">';
					html += '<button class="ork-help-item-trigger" type="button">';
					html += '<span class="ork-help-item-trigger-left">';
					html += '<i class="' + _esc(item.icon) + ' ork-help-item-icon"></i>';
					html += _esc(item.title);
					html += '</span>';
					html += '<i class="fas fa-chevron-down ork-help-chevron"></i>';
					html += '</button>';
					html += '<div class="ork-help-item-body">' + (item.body || '') + '</div>';
					html += '</div>';
				});
				html += '</div>';
			});

			_body.innerHTML = html;

			// Wire accordion
			_body.querySelectorAll('.ork-help-item-trigger').forEach(function(btn) {
				btn.addEventListener('click', function() {
					var item = this.closest('.ork-help-item');
					item.classList.toggle('ork-help-expanded');
				});
			});
		}

		// Public toggle
		window.orkHelpToggle = function() {
			if (_isOpen && !_isMinimized) { _close(); } else { _open(); }
		};

		if (document.readyState === 'loading') {
			document.addEventListener('DOMContentLoaded', _init);
		} else {
			_init();
		}
	})();
=======
	<!-- Simple info dialog -->
	<div id="nav-info-overlay" style="display:none;position:fixed;inset:0;background:rgba(0,0,0,0.45);z-index:9999;align-items:center;justify-content:center;">
		<div style="background:#fff;border-radius:10px;padding:32px 28px;max-width:360px;width:90%;box-shadow:0 8px 32px rgba(0,0,0,0.22);text-align:center;font-family:inherit;">
			<div style="font-size:32px;margin-bottom:12px;color:#4a5568;"><i class="fas fa-book"></i></div>
			<div id="nav-info-msg" style="font-size:15px;color:#2d3748;line-height:1.5;margin-bottom:20px;"></div>
			<button onclick="document.getElementById('nav-info-overlay').style.display='none';" style="background:#4299e1;color:#fff;border:none;border-radius:6px;padding:8px 28px;font-size:14px;cursor:pointer;font-family:inherit;">OK</button>
		</div>
	</div>
	<script>
	function navInfoDialog(msg) {
		document.getElementById('nav-info-msg').textContent = msg;
		var el = document.getElementById('nav-info-overlay');
		el.style.display = 'flex';
		el.onclick = function(e) { if (e.target === el) el.style.display = 'none'; };
	}
	</script>

	</body>
</html>
